spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tinyurl
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      dialect: org.hibernate.dialect.PostgreSQLDialect

